<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RetailShelfAuditApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.RetailShelfAuditApp" parent="Base.Theme.RetailShelfAuditApp" />
    <style name="PrimaryButton">
        <item name="android:backgroundTint">@color/primary_red</item>
        <item name="android:textColor">@color/background_white</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:minHeight">55dp</item>
    </style>

    <style name="SecondaryButton">
        <item name="android:backgroundTint">@color/card_background</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:minHeight">55dp</item>
    </style>
</resources>